name: Metrics
on:
  # Schedule daily updates
  schedule: [{cron: "0 0 * * *"}]
  # (optional) Run workflow manually
  workflow_dispatch:
  # (optional) Run workflow when pushing on master/main
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    environment: 
      name: production
    permissions:
      contents: write
    steps:
      - uses: M0nkeyPr0grammer/metrics@latest
        with:
          token: ${{ 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}}
